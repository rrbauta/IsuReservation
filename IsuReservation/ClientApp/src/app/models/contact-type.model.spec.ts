import {ContactType

.
Model
}
from
'./contact-type.model';

describe('ContactType.Model', () => {
  it('should create an instance', () => {
    expect(new ContactType.Model()).toBeTruthy();
  });
});
