import {Reservation

.
Paging.Model
}
from
'./reservation.paging.model';

describe('Reservation.Paging.Model', () => {
  it('should create an instance', () => {
    expect(new Reservation.Paging.Model()).toBeTruthy();
  });
});
