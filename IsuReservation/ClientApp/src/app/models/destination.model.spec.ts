import {Destination

.
Model
}
from
'./destination.model';

describe('Destination.Model', () => {
  it('should create an instance', () => {
    expect(new Destination.Model()).toBeTruthy();
  });
});
